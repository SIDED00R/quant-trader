# 주식 ML 파이프라인 전용 의존성 (batch/features, batch/ml).
# 프로덕션 이미지는 batch/ 제외라 production requirements.txt와 분리(경계 유지).
# 설치: .venv/Scripts/python -m pip install -r requirements-ml.txt
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
lightgbm>=4.0
pykrx>=1.2.8        # KRX 데이터(외국인 수급·공매도·KR PIT) — 1.2.8+ 로그인(KRX_ID/KRX_PW) 지원
setuptools<81      # Py3.13+setuptools81+ 는 pkg_resources 제거 → pykrx import 깨짐(pin 필수)
finance-datareader>=0.9   # KR 상폐종목(생존편향 보정) — FDR KRX-DELISTING 목록·OHLCV
torch>=2.0
